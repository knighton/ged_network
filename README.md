# Generator-Embedder-Discriminator Network

Modifying GAN to (a) train the generator better and (b) be able to embed the images.

```
embedding --[generator]-> image --[embedder]-> identical embedding --[discriminator]-> is real
```
